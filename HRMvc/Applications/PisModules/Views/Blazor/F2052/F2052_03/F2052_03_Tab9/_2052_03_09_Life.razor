@using HRApiLibrary.DataAccess._10_Pis.Interface
@using HRApiLibrary.DataAccess._10_Pis.OPis

@inject NotificationService _NotificationService;
@inject DialogService _DialogService;

<style>
    .s222-t1-lbl-01 {
        color: darkgreen;
        width: 20%;
    }
</style>


<div class="p-2">
    <RadzenTemplateForm TItem="OEmpmasModel" Data=@V.Empmas Submit=@OnSubmit InvalidSubmit=@OnInvalidSubmit>

        <div class="h-10p"></div>
        @if (!string.IsNullOrWhiteSpace(V.Empmas.EmpNumber))
        {
            <div class="p-2 bordered mb-2 w-100">
                <button type="submit" class="card  flex-fill text-center h-40p mb-btn mb-btn-blue p-2 px-4 ">
                    <div class="d-flex gap-1 align-items-center justify-content-center">
                        <span class="fw-semibold"><small>Save</small></span>
                    </div>
                </button>
            </div>
        }
        <RadzenFieldset>

            <div class="h-10p"></div>
            <RadzenFieldset Text="******">
                @* ----- INSURANCE (varchar(60))----------*@
                <div class="w-100 d-flex p-1">
                    <div class="w-20 small">Insurance Type </div>
                    <div class="w-80">
                        <RadzenTextBox @bind-Value=@V.Empmas.Insurance class="form-control" Name="Insurance" MaxLength="60" Placeholder="Insurance Type" />
                        <RadzenRequiredValidator Component="Insurance" Text="Insurance Type is required" Style="color: red; display:block" />
                    </div>
                </div>

                @* ----- PolicyNo (varchar(50))----------*@
                <div class="w-100 d-flex p-1">
                    <div class="w-20 small">Policy No. </div>
                    <div class="w-80">
                        <RadzenTextBox @bind-Value=@V.Empmas.PolicyNo class="form-control" Name="PolicyNo" MaxLength="50" Placeholder="Policy No." />
                        <RadzenRequiredValidator Component="PolicyNo" Text="Policy No. is required" Style="color: red; display:block" />
                    </div>
                </div>
            </RadzenFieldset>

            <div class="h-10p"></div>
            <RadzenFieldset Text="******">
                @* ----- FaceValue (decimal)----------*@
                <div class="w-100 d-flex p-1">
                    <div class="w-20 small">Face Value</div>
                    <div class="w-40">
                        <RadzenNumeric @bind-Value=@V.Empmas.FaceValue Format="#,##0.00" Placeholder="0.00" TextAlign="TextAlign.Right"
                                       Name="FaceValue"
                                       InputAttributes="@(new Dictionary<string, object>() { { "aria-label", "enter value" } })" />
                        <RadzenRequiredValidator Component="FaceValue" Text="Face Value is required" Style="color: red; display:block" />
                    </div>
                </div>

                @* ----- Premium (decimal)----------*@
                <div class="w-100 d-flex p-1">
                    <div class="w-20 small">Premium </div>
                    <div class="w-40">
                        <RadzenNumeric @bind-Value=@V.Empmas.Premium Format="#,##0.00" Placeholder="0.00" TextAlign="TextAlign.Right"
                                       Name="Premium"
                                       InputAttributes="@(new Dictionary<string, object>() { { "aria-label", "enter value" } })" />
                        <RadzenRequiredValidator Component="Premium" Text="Premium is required" Style="color: red; display:block" />
                    </div>
                </div>
            </RadzenFieldset>

            <div class="h-10p">&nbsp</div>
            <RadzenFieldset Text="******">
                @* ----- InsExpire (date)----------*@
                <div class="w-100 d-flex p-1">
                    <div class="w-20 small">Expiration Date </div>
                    <div class="w-40">
                        <RadzenDatePicker @bind-Value=@V.Empmas.InsExpire Name="InsExpire" DateFormat="MMM dd, yyyy" Style="width:100%" />
                        <RadzenRequiredValidator Component="InsExpire" Text="Expiration Date is required" Style="color: red; display:block" />
                    </div>
                </div>
            </RadzenFieldset>

        </RadzenFieldset>
    </RadzenTemplateForm>

</div>

@inject IOEmpmasDataAccess _empmas

@code {
    [Parameter]
    public V2052 V { get; set; } = new();

    [Parameter]
    public EventCallback ChildAction { get; set; }

    async Task OnSubmit(OEmpmasModel model)
    {
        if (model == null) return;
        try
        {
            await _empmas._03_Insurance(V.SelectedEmpnumber, V.Empmas, V.pisdb, V.conn);

            _NotificationService.Notify(new NotificationMessage
            {
                Severity = NotificationSeverity.Success,
                Summary = "Successfully saved",
                Detail = "Record has been successfully saved.",
                Duration = 3000
            });
        }
        catch (Exception ex)
        {
            Console.WriteLine(ex);
            _NotificationService.Notify(new NotificationMessage
            {
                Severity = NotificationSeverity.Error,
                Summary = "Unable to save",
                Detail = "Something went wrong while saving. Please try again.",
                Duration = 6000
            });
        }
    }

    void OnInvalidSubmit(FormInvalidSubmitEventArgs args)
    {

    }
}
