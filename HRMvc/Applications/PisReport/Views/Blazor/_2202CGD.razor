@using HRApiLibrary.DataAccess._20_Pay.Interface
@using HRMvc.Applications.PisReport.Views.Blazor._2202
<_00BT_FullW HdrCaption="@rv.MCaption" Rv="@rv">
    <Details>
        @*--- Looader ------------------------------------------------*@
        <_00Loader Rv="@rv" />


        <_2202_Profile />
        <_2202_Government />
        <_2202_Education />
        <_2202_Referral />
        <_2202_EmploymentRecord />
        <_2202_MilitaryRecord />
        <_2202_PersonalData />
        <_2202_Family />
        <_2202_Skills />
        <_2202_SeminarsAndTrainings />
        <_2202_BackgroundDeclaration />
        <_2202_References />
        <_2202_EmergencyContact />
           
       
    </Details>
</_00BT_FullW>

@inject ISettingsDataAccess _settings;

@code {
    [Parameter] public UserClaimsModel Uc { get; set; } = new();
    V00_RepVars rv = new();

    protected override async Task OnInitializedAsync()
    {
        InitialiazeReport();
        await LoadInitialData();
    }


    private void InitialiazeReport()
    {
        rv.RefreshToken = 22020000;
        rv.MCaption = "Application Form";
        rv.ReportParams = new()
        {
            ["CoName"] = "-",
            ["CoAddress"] = "-",
            ["ReportTitle"] = "Personal Data Sheet",
            ["RunDate"] = DateTime.Now
        };
    }

    private async Task LoadInitialData()
    {
        await  LoadCompanyInfo();
    }

    private async Task LoadCompanyInfo()
    {
        var res = await _settings._02(1, Uc.SchemaUserPay, Uc.Conn);
    }
}


<style>
    .fields {
        display: flex;
        flex: 1;
        gap: 15px;
    }

    .field {
        flex: 1;
        align-items: center;
    }

    .label {
        min-width: auto;
        padding-top: 8px;
        white-space: nowrap;
        flex-shrink: 0;
        padding-inline-end: 1rem;
    }

    .sub-label {
        font-size: 12px;
        margin-top: 4px;
        text-align: center;
    }

    .line {
        display: flex;
        flex: 1;
        border-bottom: 1px solid #bdbdbd;
        height: 25px;
    }


    .char-box {
        width: 25px;
        height: 30px;
        border: 1px solid #bdbdbd;
        display: inline-flex;
        justify-content: center;
        align-items: center;
    }
</style>
