@using HRMvc.Applications.PisReport.Vars
<div class="">
    @* --- Header ---------------------- *@
    <div class=" d-flex p-2 border-bottom fw-bold ">
        <div class="w-100p">    No</div>
        <div class="w-150p">    Emp. No.</div>
        <div class="w-250p pe-3">Emp. Name</div>
        <div class="w-150p">    Status</div>
        <div class="w-250p">License No.</div>
        <div class="w-250p">License Exp</div>
        <div class="w-350p">    Deployment</div>
        
    </div>
    
    @* --- Details ---------------------- *@
    <div class="vh-max vh75">
        @{

            int row = 0; int ctr = 1;
            if(V.Count() > 0)
            {
                foreach (var v in V)
                {
                    <div class=" d-flex p-2 border-bottom @(row % 2 == 0 ? "bg-light" : "") ">
                        <div class="w-100p">        @ctr </div>
                        <div class="w-150p">        @v.EmpNumber </div>
                        <div class="w-250p pe-3">   @v.EmpName </div>
                        <div class="w-150p">        @v.EmpStatus </div>
                        <div class="w-250p">        @v.SecLicense </div>
                        <div class="w-250p">        @(v.LicExpire?.ToString("MM/dd/yyyy") ?? "") </div>
                        <div class="w-350p">        @v.ClName </div>
                    </div>
                    row++; ctr++;
                }
            }
            else
            {

                <div class="d-flex justify-content-center align-items-center w-100 h-100 pt-5 ">
                    No records to display.
                </div>
                
            }
           
        }
    </div>
  


</div>


@code {
    [Parameter] public List<V2217Model> V {get; set;}=[];
  

}