<<<<<<< HEAD
@inject UserClaimsContextService ClaimsContext
@inject L12_102 L102 
=======
@using System.Security.Claims
@using HRApiLibrary.DataAccess._00_Main.Interface
@using HRApiLibrary.DataAccess._10_Pis.OPis
@using HRApiLibrary.Models._00_Main
@using HRApiLibrary.Models._10_Pis.OPis
@using Microsoft.AspNetCore.Http
@using Microsoft.Extensions.Configuration

@inject I_00MainDA                  _mainDA
@inject IConfiguration              _config;
@inject IOEmpmasDataAccess          _OEmpmas;
>>>>>>> DA_mar


@{
    Layout = "~/Views/Shared/Main/_ReportLayout2.cshtml";
<<<<<<< HEAD
    var userClaims  = ClaimsContext.Build(User);
    var OEmpmas = await L102._02OEmpmass(userClaims); 
    var V12_102             = await L102.V12_102(userClaims);
=======

    var userid = User.Claims.Where(c => c.Type == "UserId").FirstOrDefault();
    IEnumerable<Claim?> claims = User.Claims;
    UserClaimsModel userClaims = _mainDA._02UserClaimsContent(claims);
    
    var oEmpnumber = Context.Session.GetString("EmpNumber");
    var oPisDb = Context.Session.GetString("OldPis");
    var oPayDb = Context.Session.GetString("OldPis");
    
    userClaims.OempNumber = oEmpnumber;
    userClaims.OpayDb = oPisDb;
    userClaims.OpisDb = oPisDb; 
    
    List<OEmpmasModel?>? OEmpmas = new(); 
    
    /*if(string.IsNullOrEmpty(empnumber)) return;
    OEmpmas = await _OEmpmas._02(empnumber, oldPis, userClaims.Conn );*/



>>>>>>> DA_mar
}

<div>
    
    
    @(await Html.RenderComponentAsync<_12_102DB>(RenderMode.ServerPrerendered, 
        new { UserClaims = userClaims, OEmpmass = OEmpmas, V12_102 = V12_102 }))

</div>



<script>
    window.startClock = function (elementId) {
        setInterval(function () {
            const now       = new Date();
            const options   = { weekday: 'long', year: 'numeric', month: 'long', day: '2-digit', hour: '2-digit', minute: '2-digit', second: '2-digit' };

            const el = document.getElementById(elementId);
            if (el) { el.innerText = now.toLocaleString('en-US', options); }

        }, 1000);

    }

    window.startTextboxTime = function (elementId) {
        setInterval(function () 
        {
            const now = new Date();
            const options = { hour: '2-digit', minute: '2-digit', second: '2-digit' };
            const el = document.getElementById(elementId);
        
            if (el) { el.value = now.toLocaleTimeString('en-US', options); }

        }, 1000);

    }
</script>




@* <script>
    window.startClock = function (elementId) {

        setInterval(function () {

            const now = new Date();

            const options = {
                weekday: 'long',
                year: 'numeric',
                month: 'long',
                day: '2-digit',
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit'
            };

            const el = document.getElementById(elementId);
            if (el) {
                el.innerText = now.toLocaleString('en-US', options);
            }

        }, 1000);

    }
</script> *@