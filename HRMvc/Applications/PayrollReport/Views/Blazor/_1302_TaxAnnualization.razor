@if(showModal) { <_00Modal ChildAction="@ShowHideModal" ModuleCaption="Tax Annualization"> </_00Modal> }


@code {

    [Parameter] public UserClaimsModel? UserClaims { get; set; }

    bool showModal = false; 

    void ShowHideModal(bool ans) {
        showModal = ans; 
    }

}
