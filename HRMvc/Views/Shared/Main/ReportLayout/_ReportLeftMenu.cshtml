@model HRApiLibrary.Models._00_Main.UserClaimsModel

@using HRApiLibrary.DataAccess._90_Utils
@inject IMainmenuDataAccess _menu

@{
    // Load menu list ONCE
    var menus    = await _menu._02("MainPay", Model.Conn ?? "") ?? [];
    var menuspis = await _menu._02("MainPis", Model.Conn ?? "") ?? [];

    const string childCshtml = "~/Views/Shared/Main/ReportLayout/_ReportLeftMenuDtl1.cshtml";
}

<!-- Custom CSS -->
<link rel="stylesheet" href="~/css/ReportLayout/report-left-menu.css" />

<div class="py-2 px-3 fw-bold bordered  report-left-menu">Employee Portal</div>




<div class="py-2 px-3 fw-bold bordered  report-left-menu">PIS Report</div>
<div class="report-left-menu">
    <ul>
        @await Html.PartialAsync(childCshtml, (menuspis, 2101))
        @await Html.PartialAsync(childCshtml, (menuspis, 2201))
    </ul>
</div>


<div class="py-2 px-3 fw-bold bordered-bottom  report-left-menu mt-2">Payroll Report</div>
<div class="report-left-menu">
    <ul>
        @await Html.PartialAsync(childCshtml, (menus, 1101))
        @await Html.PartialAsync(childCshtml, (menus, 1151))
        @await Html.PartialAsync(childCshtml, (menus, 1201))
        @await Html.PartialAsync(childCshtml, (menus, 1251))
        @await Html.PartialAsync(childCshtml, (menus, 1301))
    </ul>
</div>





